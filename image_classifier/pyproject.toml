[project]
name = "mcp-image-classifier"
version = "0.1.0"
description = "Image classification MCP server for handwritten digits (MNIST)"
readme = "README.md"
authors = [{ name = "hiro8ma" }]
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.14.1",
    "torch>=2.0.0",
    "torchvision>=0.15.0",
    "pillow>=10.0.0",
    "numpy>=1.24.0",
]
